{{- include "merge_presets" . -}}
apiVersion: v1
kind: Namespace
metadata:
  name: mynewapp
  labels:
    kubernetes.io/metadata.name: mynewapp
    my.escaped/label: must-not-be-cleared
spec:
  finalizers:
  - kubernetes
